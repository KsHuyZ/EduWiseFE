import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Certificate',
  description: 'Cerficate',
};

const Layout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default Layout;
